apiVersion: v1
kind: Service

metadata:
  name: echo-service
  namespace: echo-ns

spec:
  selector:
    app: echo-pod
  ports:
    - port: 80  #Service port
      targetPort: 80  #Container Port
      nodePort: 30101 # orta esposta del nodo
  type: NodePort

      

